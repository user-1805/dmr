library(arules)

wholesale <- read.transactions("wholesale.csv", format = "basket",sep = ",")
summary(wholesale)
rules <- apriori(wholesale,parameter = list(supp = 0.2, conf = 0.5))

rules_sorted <- sort(rules, by = "confidence", decreasing = TRUE)
inspect(head(rules_sorted, 5))
